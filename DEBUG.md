# راهنمای عیب‌یابی خطای افزودن دارو

## بررسی‌های اولیه

### 1. بررسی کنید Backend در حال اجراست

در ترمینال ببینید که این پیام را دارید:
```
🚀 Server running on http://localhost:3001
📊 API endpoints available at http://localhost:3001/api
```

اگر نه، اجرا کنید:
```bash
npm run server
```

### 2. بررسی اتصال به دیتابیس

مطمئن شوید که:
- PostgreSQL در حال اجراست: `sudo systemctl status postgresql`
- جداول در دیتابیس ایجاد شده‌اند: `npm run db:studio`
- Prisma Client تولید شده: `npm run db:generate`

### 3. بررسی Console در مرورگر

در Developer Tools مرورگر (F12):
- تب Console را باز کنید
- هنگام افزودن دارو، خطاها را ببینید
- تب Network را باز کنید و ببینید که درخواست POST به `/api/medications` ارسال می‌شود یا نه

### 4. بررسی Console در Backend

در ترمینال Backend، خطاها را ببینید. خطاها باید نمایش داده شوند.

## تست مستقیم API

می‌توانید API را مستقیماً با curl تست کنید:

```bash
curl -X POST http://localhost:3001/api/medications \
  -H "Content-Type: application/json" \
  -d '{
    "name": "پاراستامول",
    "dosageForm": "قرص",
    "strength": "500mg",
    "manufacturer": "تست",
    "price": 10000,
    "stock": 100
  }'
```

یا با یک نمونه داده کامل:

```bash
curl -X POST http://localhost:3001/api/medications \
  -H "Content-Type: application/json" \
  -d '{
    "name": "آزمایش دارو",
    "genericName": "تست",
    "dosageForm": "قرص",
    "strength": "500mg",
    "manufacturer": "شرکت تست",
    "price": 50000,
    "stock": 50,
    "description": "این یک داروی تست است"
  }'
```

## مشکلات رایج

### مشکل 1: Backend در حال اجرا نیست
**راه‌حل**: 
```bash
npm run server
```

### مشکل 2: دیتابیس راه‌اندازی نشده
**راه‌حل**:
```bash
npm run db:generate
npm run db:push
```

### مشکل 3: خطای CORS
**راه‌حل**: در `server/src/server.ts` مطمئن شوید که CORS درست تنظیم شده.

### مشکل 4: فیلدهای الزامی خالی هستند
**راه‌حل**: مطمئن شوید که:
- نام دارو پر شده
- فرم دارویی انتخاب شده
- میزان دارو پر شده

### مشکل 5: خطای Prisma Client
**راه‌حل**:
```bash
npm run db:generate
```

## بررسی دقیق‌تر

اگر هنوز مشکل دارید، این اطلاعات را جمع‌آوری کنید:

1. **خطای Console در مرورگر** (F12 → Console)
2. **خطای Backend** (در ترمینال که server را اجرا کرده‌اید)
3. **Request در Network Tab** (F12 → Network → درخواست POST)
4. **Response از Backend** (در Network Tab → Response)



