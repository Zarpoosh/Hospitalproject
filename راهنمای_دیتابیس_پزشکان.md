# راهنمای ذخیره پزشکان و تخصص‌ها در دیتابیس

## ✅ کارهای انجام شده

1. ✅ API Routes برای پزشکان ساخته شد (`server/src/routes/doctors.ts`)
2. ✅ API Client در Frontend ساخته شد (`src/utils/doctorApi.ts`)
3. ✅ Hook به‌روزرسانی شد تا از API استفاده کند
4. ✅ کامپوننت‌ها به‌روزرسانی شدند

## 🚀 مراحل راه‌اندازی

### مرحله 1: اجرای Seed برای اضافه کردن پزشکان

برای اضافه کردن پزشکان نمونه به دیتابیس:

```bash
npm run db:seed
```

یا:

```bash
npm run db:seed:doctors
```

این دستور 13 پزشک با تخصص‌های مختلف به دیتابیس اضافه می‌کند.

### مرحله 2: اجرای Backend

```bash
npm run server
```

### مرحله 3: تست API

در ترمینال می‌توانید تست کنید:

```bash
# دریافت همه پزشکان
curl http://localhost:3001/api/doctors

# دریافت پزشکان یک تخصص خاص
curl http://localhost:3001/api/doctors/specialization/متخصص%20داخلی

# دریافت لیست تخصص‌ها
curl http://localhost:3001/api/doctors/specializations/list
```

### مرحله 4: استفاده در Frontend

حالا در پنل بیمار:
1. به بخش "رزرو نوبت جدید" بروید
2. تخصص‌ها از دیتابیس بارگذاری می‌شوند
3. با انتخاب تخصص، پزشکان آن تخصص از دیتابیس نمایش داده می‌شوند

## 📊 API Endpoints

### پزشکان

- `GET /api/doctors` - دریافت همه پزشکان
- `GET /api/doctors/:id` - دریافت یک پزشک خاص
- `GET /api/doctors/specialization/:specialization` - دریافت پزشکان یک تخصص
- `GET /api/doctors/specializations/list` - دریافت لیست تخصص‌ها

## 🔧 اضافه کردن پزشک جدید به دیتابیس

### روش 1: استفاده از Prisma Studio

```bash
npm run db:studio
```

سپس:
1. جدول `User` را باز کنید
2. یک کاربر جدید با role="doctor" اضافه کنید
3. جدول `Doctor` را باز کنید
4. یک رکورد جدید اضافه کنید و userId را به کاربر متصل کنید

### روش 2: استفاده از Seed

فایل `prisma/seed.ts` را ویرایش کنید و پزشک جدید اضافه کنید، سپس:

```bash
npm run db:seed
```

### روش 3: استفاده از SQL مستقیم

```sql
-- ایجاد کاربر
INSERT INTO "User" (username, password, role, name, email, phone)
VALUES ('doctor14', 'doctor123', 'doctor', 'دکتر جدید', 'new@hospital.com', '09123456802');

-- ایجاد پزشک
INSERT INTO "Doctor" ("userId", specialization, "availableDays", "availableHours", "consultationFee")
VALUES (
  (SELECT id FROM "User" WHERE username = 'doctor14'),
  'متخصص داخلی',
  '["شنبه", "یکشنبه"]',
  '["08:00", "09:00", "10:00"]',
  150000
);
```

## 📝 نکات مهم

1. **availableDays و availableHours**: این فیلدها به صورت JSON string ذخیره می‌شوند
2. **تخصص‌ها**: به صورت خودکار از پزشکان موجود در دیتابیس استخراج می‌شوند
3. **Fallback**: اگر API خطا بدهد، از لیست پیش‌فرض استفاده می‌شود

## 🐛 عیب‌یابی

**مشکل**: پزشکان نمایش داده نمی‌شوند
- بررسی کنید Seed اجرا شده: `npm run db:seed`
- بررسی کنید Backend در حال اجراست: `npm run server`
- بررسی کنید دیتابیس: `npm run db:studio`

**مشکل**: تخصص‌ها خالی است
- بررسی کنید که پزشکانی در دیتابیس وجود دارند
- بررسی کنید API endpoint: `curl http://localhost:3001/api/doctors/specializations/list`

## ✨ مزایا

✅ داده‌ها در دیتابیس ذخیره می‌شوند
✅ می‌توانید پزشکان جدید اضافه کنید
✅ تخصص‌ها به صورت خودکار از دیتابیس استخراج می‌شوند
✅ امکان فیلتر بر اساس تخصص
✅ داده‌ها پایدار هستند و با رفرش صفحه از بین نمی‌روند

