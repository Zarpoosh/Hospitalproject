# 📚 مستند کامل پروژه سیستم مدیریت بیمارستان

## 📋 فهرست مطالب

1. [معرفی پروژه](#معرفی-پروژه)
2. [معماری کلی پروژه](#معماری-کلی-پروژه)
3. [بخش‌های مختلف پروژه](#بخش‌های-مختلف-پروژه)
4. [دیتابیس و مدل داده](#دیتابیس-و-مدل-داده)
5. [بک‌اند (Backend)](#بک‌اند-backend)
6. [فرانت‌اند (Frontend)](#فرانت‌اند-frontend)
7. [نحوه اجرای پروژه](#نحوه-اجرای-پروژه)
8. [ساختار فایل‌ها](#ساختار-فایل‌ها)
9. [تکنولوژی‌های استفاده شده](#تکنولوژی‌های-استفاده-شده)

---

## 🏥 معرفی پروژه

این پروژه یک **سیستم مدیریت بیمارستان** است که برای مدیریت اطلاعات بیماران، پزشکان، داروسازان، قرارهای ملاقات و نسخه‌ها طراحی شده است.

### ویژگی‌های اصلی:
- ✅ مدیریت کاربران با نقش‌های مختلف (بیمار، پزشک، داروساز، مدیر)
- ✅ سیستم رزرو نوبت برای بیماران
- ✅ مدیریت داروخانه و موجودی داروها
- ✅ سیستم نسخه‌نویسی برای پزشکان
- ✅ داشبوردهای اختصاصی برای هر نقش

---

## 🏗️ معماری کلی پروژه

این پروژه از معماری **Full-Stack** با جداسازی Frontend و Backend استفاده می‌کند:

```
┌─────────────────┐
│   Frontend      │  React + TypeScript + Vite
│   (Port 5173)   │  └─ UI Components
│                 │  └─ State Management
│                 │  └─ API Calls
└────────┬────────┘
         │ HTTP/REST API
         │
┌────────▼────────┐
│   Backend       │  Express + TypeScript
│   (Port 3001)   │  └─ REST API Routes
│                 │  └─ Business Logic
└────────┬────────┘
         │
┌────────▼────────┐
│   Database      │  PostgreSQL
│   (Port 5432)   │  └─ Relational Database
│                 │  └─ Managed by Prisma ORM
└─────────────────┘
```

### جریان داده:
1. **Frontend** درخواست HTTP به **Backend** ارسال می‌کند
2. **Backend** با استفاده از **Prisma ORM** به **PostgreSQL** متصل می‌شود
3. داده‌ها پردازش شده و به صورت JSON به **Frontend** برگردانده می‌شوند
4. **Frontend** داده‌ها را نمایش می‌دهد

---

## 📦 بخش‌های مختلف پروژه

### 1. **Frontend (فرانت‌اند)**
- **مکان**: پوشه `src/`
- **تکنولوژی**: React 19 + TypeScript + Vite
- **UI Framework**: Tailwind CSS v4
- **Routing**: React Router v6
- **Icons**: Lucide React

### 2. **Backend (بک‌اند)**
- **مکان**: پوشه `server/src/`
- **تکنولوژی**: Express.js + TypeScript
- **Port**: 3001
- **API Style**: RESTful API

### 3. **Database (دیتابیس)**
- **نوع**: PostgreSQL (Relational Database)
- **ORM**: Prisma
- **Schema**: `prisma/schema.prisma`
- **Port**: 5432

---

## 🗄️ دیتابیس و مدل داده

### نوع دیتابیس: **Relational Database (PostgreSQL)**

این پروژه از **PostgreSQL** استفاده می‌کند که یک دیتابیس **رابطه‌ای (Relational)** است.

#### تفاوت دیتابیس رابطه‌ای و غیررابطه‌ای:

| ویژگی | دیتابیس رابطه‌ای (PostgreSQL) | دیتابیس غیررابطه‌ای (MongoDB) |
|------|-------------------------------|-------------------------------|
| **ساختار داده** | جداول با سطر و ستون | اسناد JSON |
| **روابط** | Foreign Key و Join | Embedding یا Reference |
| **Schema** | ثابت و تعریف شده | انعطاف‌پذیر |
| **Query Language** | SQL | Query Language خاص |
| **ACID** | پشتیبانی کامل | محدود |
| **مثال** | PostgreSQL, MySQL | MongoDB, Redis |

#### چرا PostgreSQL برای این پروژه مناسب است؟
- ✅ نیاز به روابط پیچیده بین جداول (User → Patient/Doctor)
- ✅ نیاز به یکپارچگی داده‌ها (Foreign Key Constraints)
- ✅ نیاز به Transaction برای عملیات‌های مهم
- ✅ نیاز به Query‌های پیچیده با JOIN

### مدل‌های داده (Prisma Schema):

#### 1. **User (کاربر)**
```prisma
model User {
  id          Int      @id @default(autoincrement())
  username    String   @unique
  password    String
  role        String   // patient, doctor, pharmacist, admin
  name        String
  email       String?
  phone       String?
  
  // روابط یک به یک
  patient     Patient?
  doctor      Doctor?
  pharmacist  Pharmacist?
  admin       Admin?
}
```

#### 2. **Patient (بیمار)**
```prisma
model Patient {
  id          Int      @id @default(autoincrement())
  userId      Int      @unique
  user        User     @relation(...)
  nationalId  String   @unique
  dateOfBirth DateTime
  
  // روابط یک به چند
  appointments Appointment[]
  prescriptions Prescription[]
}
```

#### 3. **Doctor (پزشک)**
```prisma
model Doctor {
  id              Int      @id @default(autoincrement())
  userId          Int      @unique
  user            User     @relation(...)
  specialization  String
  availableDays   String   // JSON array
  availableHours  String   // JSON array
  consultationFee Int
  
  // روابط یک به چند
  appointments    Appointment[]
  prescriptions   Prescription[]
}
```

#### 4. **Medication (دارو)**
```prisma
model Medication {
  id            Int      @id @default(autoincrement())
  name          String
  genericName   String?
  dosageForm    String
  strength      String
  price         Int?
  stock         Int?
  
  // روابط یک به چند
  prescriptionItems PrescriptionItem[]
}
```

#### 5. **Appointment (قرار ملاقات)**
```prisma
model Appointment {
  id        Int      @id @default(autoincrement())
  patientId Int
  doctorId  Int
  patient   Patient  @relation(...)
  doctor    Doctor   @relation(...)
  date      DateTime
  time      String
  status    String   @default("pending")
  reason    String?
}
```

#### 6. **Prescription (نسخه)**
```prisma
model Prescription {
  id          Int      @id @default(autoincrement())
  patientId   Int
  doctorId    Int
  patient     Patient  @relation(...)
  doctor      Doctor   @relation(...)
  date        DateTime @default(now())
  diagnosis   String
  status      String   @default("pending")
  
  // روابط یک به چند
  items       PrescriptionItem[]
}
```

### روابط بین جداول:

```
User (1) ──→ (1) Patient
User (1) ──→ (1) Doctor
User (1) ──→ (1) Pharmacist
User (1) ──→ (1) Admin

Patient (1) ──→ (N) Appointment
Doctor (1) ──→ (N) Appointment

Patient (1) ──→ (N) Prescription
Doctor (1) ──→ (N) Prescription

Prescription (1) ──→ (N) PrescriptionItem
Medication (1) ──→ (N) PrescriptionItem
```

---

## 🔧 بک‌اند (Backend)

### ساختار Backend:

```
server/
├── src/
│   ├── server.ts          # نقطه ورود سرور
│   ├── db.ts              # اتصال به دیتابیس با Prisma
│   ├── routes/
│   │   ├── doctors.ts     # API endpoints برای پزشکان
│   │   └── medications.ts # API endpoints برای داروها
│   └── middleware/        # Middleware های Express
```

### API Endpoints:

#### پزشکان (Doctors):
- `GET /api/doctors` - دریافت همه پزشکان
- `GET /api/doctors/specialization/:specialization` - دریافت پزشکان بر اساس تخصص
- `GET /api/doctors/:id` - دریافت یک پزشک خاص

#### داروها (Medications):
- `GET /api/medications` - دریافت همه داروها
- `GET /api/medications/:id` - دریافت یک دارو خاص
- `POST /api/medications` - اضافه کردن داروی جدید
- `PUT /api/medications/:id` - به‌روزرسانی دارو
- `DELETE /api/medications/:id` - حذف دارو

#### Health Check:
- `GET /api/health` - بررسی وضعیت سرور

### مثال کد Backend:

```typescript
// server/src/routes/doctors.ts
import express, { Request, Response } from 'express';
import prisma from '../db';

const router = express.Router();

// دریافت همه پزشکان
router.get('/', async (req: Request, res: Response) => {
  try {
    const doctors = await prisma.doctor.findMany({
      include: {
        user: {
          select: {
            id: true,
            name: true,
            email: true,
          },
        },
      },
    });
    res.json(doctors);
  } catch (error) {
    res.status(500).json({ error: 'خطا در دریافت پزشکان' });
  }
});
```

---

## 🎨 فرانت‌اند (Frontend)

### ساختار Frontend:

```
src/
├── pages/              # صفحات اصلی
│   ├── Login.tsx
│   ├── PatientDashboard.tsx
│   ├── DoctorDashboard.tsx
│   └── PharmacistDashboard.tsx
├── components/         # کامپوننت‌های React
│   ├── Patient/       # کامپوننت‌های پنل بیمار
│   ├── Doctor/        # کامپوننت‌های پنل پزشک
│   ├── Pharmacist/    # کامپوننت‌های پنل داروساز
│   ├── Auth/          # کامپوننت‌های احراز هویت
│   └── Common/        # کامپوننت‌های مشترک
├── hooks/              # Custom Hooks
│   ├── useAuth.ts
│   ├── usePatientDashboard.ts
│   ├── useDoctorDashboard.ts
│   └── usePharmacistDashboard.ts
├── utils/              # توابع کمکی
│   ├── api.ts
│   ├── doctorApi.ts
│   └── medicationApi.ts
└── constants/          # ثابت‌ها
    ├── auth.ts
    ├── patient.ts
    └── doctor.ts
```

### Routing:

```typescript
// src/App.tsx
<Routes>
  <Route path="/login" element={<Login />} />
  <Route path="/patient/dashboard" element={
    <ProtectedRoute allowedRoles={['patient']}>
      <PatientDashboard />
    </ProtectedRoute>
  } />
  <Route path="/doctor/dashboard" element={
    <ProtectedRoute allowedRoles={['doctor']}>
      <DoctorDashboard />
    </ProtectedRoute>
  } />
  <Route path="/pharmacist/dashboard" element={
    <ProtectedRoute allowedRoles={['pharmacist']}>
      <PharmacistDashboard />
    </ProtectedRoute>
  } />
</Routes>
```

### State Management:

پروژه از **React Hooks** و **Local Storage** برای مدیریت state استفاده می‌کند:
- `useState` برای state محلی
- `useEffect` برای side effects
- `localStorage` برای ذخیره اطلاعات کاربر

### مثال کد Frontend:

```typescript
// src/hooks/usePatientDashboard.ts
export const usePatientDashboard = (patientId: string) => {
  const [appointments, setAppointments] = useState<Appointment[]>([]);
  const [doctors, setDoctors] = useState<Doctor[]>([]);
  
  useEffect(() => {
    fetchDoctors();
    fetchAppointments();
  }, []);
  
  const fetchDoctors = async () => {
    const data = await doctorApi.getAll();
    setDoctors(data);
  };
  
  return { appointments, doctors, ... };
};
```

---

## 🚀 نحوه اجرای پروژه

### پیش‌نیازها:
- Node.js (v18 یا بالاتر)
- PostgreSQL (v14 یا بالاتر)
- npm یا yarn

### مرحله ۱: نصب PostgreSQL

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install postgresql postgresql-contrib
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

### مرحله ۲: ایجاد دیتابیس

```bash
sudo -u postgres psql
```

در محیط PostgreSQL:
```sql
CREATE DATABASE hospital_db;
CREATE USER hospital_user WITH PASSWORD 'hospital123';
GRANT ALL PRIVILEGES ON DATABASE hospital_db TO hospital_user;
\q
```

### مرحله ۳: ایجاد فایل .env

در روت پروژه، فایل `.env` ایجاد کنید:

```env
DATABASE_URL="postgresql://hospital_user:hospital123@localhost:5432/hospital_db?schema=public"
PORT=3001
```

### مرحله ۴: نصب وابستگی‌ها

```bash
npm install
```

### مرحله ۵: راه‌اندازی دیتابیس

```bash
# تولید Prisma Client
npm run db:generate

# ایجاد جداول در دیتابیس
npm run db:migrate

# (اختیاری) اضافه کردن داده‌های نمونه
npm run db:seed
```

### مرحله ۶: اجرای پروژه

**ترمینال ۱ - Backend:**
```bash
npm run server
```
Backend در آدرس `http://localhost:3001` اجرا می‌شود.

**ترمینال ۲ - Frontend:**
```bash
npm run dev
```
Frontend در آدرس `http://localhost:5173` اجرا می‌شود.

### مشاهده دیتابیس:

```bash
npm run db:studio
```
Prisma Studio در آدرس `http://localhost:5555` باز می‌شود.

---

## 📁 ساختار فایل‌ها

```
Hospitalproject/
├── prisma/
│   ├── schema.prisma      # Schema دیتابیس
│   └── seed.ts            # داده‌های نمونه
├── server/
│   └── src/
│       ├── server.ts      # سرور Express
│       ├── db.ts          # اتصال Prisma
│       └── routes/        # API Routes
├── src/
│   ├── pages/             # صفحات React
│   ├── components/        # کامپوننت‌ها
│   ├── hooks/             # Custom Hooks
│   ├── utils/             # توابع کمکی
│   └── constants/         # ثابت‌ها
├── public/                # فایل‌های استاتیک
├── package.json           # وابستگی‌ها
├── vite.config.ts         # تنظیمات Vite
└── tsconfig.json          # تنظیمات TypeScript
```

---

## 🛠️ تکنولوژی‌های استفاده شده

### Frontend:
- **React 19.2.0** - کتابخانه UI
- **TypeScript 5.9.3** - زبان برنامه‌نویسی
- **Vite 7.2.4** - Build Tool
- **React Router 6.30.2** - Routing
- **Tailwind CSS 4.1.17** - CSS Framework
- **Lucide React 0.556.0** - آیکون‌ها

### Backend:
- **Express 4.21.2** - Web Framework
- **TypeScript 5.9.3** - زبان برنامه‌نویسی
- **Prisma 7.2.0** - ORM
- **PostgreSQL** - دیتابیس
- **CORS 2.8.5** - مدیریت CORS
- **dotenv 16.4.7** - مدیریت متغیرهای محیطی

### Database:
- **PostgreSQL** - دیتابیس رابطه‌ای
- **Prisma Client** - ORM برای دسترسی به دیتابیس

---

## 📝 دستورات مفید

### دیتابیس:
```bash
npm run db:generate    # تولید Prisma Client
npm run db:migrate     # اجرای Migration
npm run db:push        # Push تغییرات به دیتابیس
npm run db:studio      # باز کردن Prisma Studio
npm run db:seed        # اضافه کردن داده‌های نمونه
```

### توسعه:
```bash
npm run dev            # اجرای Frontend
npm run server         # اجرای Backend
npm run build          # Build پروژه
npm run lint           # بررسی کد
```

---

## 🔐 نقش‌های کاربری

### 1. **بیمار (Patient)**
- رزرو نوبت با پزشک
- مشاهده قرارهای ملاقات
- ویرایش/لغو نوبت

### 2. **پزشک (Doctor)**
- مشاهده قرارهای ملاقات
- مشاهده لیست بیماران
- نوشتن نسخه
- ویرایش/لغو قرار ملاقات

### 3. **داروساز (Pharmacist)**
- مدیریت داروها (CRUD)
- مشاهده موجودی داروها
- به‌روزرسانی قیمت و موجودی

### 4. **مدیر (Admin)**
- دسترسی کامل به سیستم
- مدیریت کاربران

---

## 🔄 جریان کار (Workflow)

### رزرو نوبت:
1. بیمار وارد پنل می‌شود
2. تخصص پزشک را انتخاب می‌کند
3. پزشک مورد نظر را انتخاب می‌کند
4. تاریخ و زمان را انتخاب می‌کند
5. علت مراجعه را وارد می‌کند
6. نوبت ثبت می‌شود

### نسخه‌نویسی:
1. پزشک وارد پنل می‌شود
2. بیمار را انتخاب می‌کند
3. نسخه را می‌نویسد
4. داروها و دوزها را مشخص می‌کند
5. نسخه ثبت می‌شود

### مدیریت دارو:
1. داروساز وارد پنل می‌شود
2. داروی جدید اضافه می‌کند یا داروی موجود را ویرایش می‌کند
3. قیمت و موجودی را به‌روزرسانی می‌کند

---

## 🐛 عیب‌یابی

### مشکل: خطای اتصال به دیتابیس
```bash
# بررسی وضعیت PostgreSQL
sudo systemctl status postgresql

# بررسی اتصال
psql -U hospital_user -d hospital_db
```

### مشکل: Backend اجرا نمی‌شود
- بررسی کنید PORT 3001 آزاد است
- بررسی کنید `.env` فایل وجود دارد
- بررسی کنید `npm install` اجرا شده است

### مشکل: Frontend به Backend متصل نمی‌شود
- بررسی کنید Backend در حال اجراست
- بررسی کنید CORS درست تنظیم شده
- بررسی کنید URL API درست است

---

## 📚 منابع و مستندات

- [Prisma Documentation](https://www.prisma.io/docs)
- [React Documentation](https://react.dev)
- [Express Documentation](https://expressjs.com)
- [PostgreSQL Documentation](https://www.postgresql.org/docs)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)

---

## 📞 پشتیبانی

برای سوالات و مشکلات:
1. بررسی فایل‌های راهنما در پروژه
2. بررسی Console مرورگر و Terminal
3. بررسی Logs در Backend

---

**آخرین به‌روزرسانی**: 2024

